#include <stdio.h>
#include <stdlib.h>
#include <conio2.h>

void draw_horizontal_line(int x_position,int y_position,int length)
{
  int counter = 0, mcm = 0;

  gotoxy(x_position,y_position);

  for

  if (length % 5 == 0)
  {
    for(counter = length; conter > 0; counter = counter-5)
    {
      printf("%c",196);
    }
  } else
  if (length % 3 == 0 )
  {
    for(counter = length; conter > 0; counter = counter-5)
    {
      printf("%c",196);
    }
  }
}
