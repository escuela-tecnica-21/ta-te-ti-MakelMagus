#include <stdio.h>
#include <stdlib.h>

//draws a herozontal line from the determined x and y coordinates.
//it does so faster if it's a multiple of a number below 8.
void draw_vertical_line(int x_position, int y_position, int length)
//draws a line from the top to down, starting from the determined coordinates.
void draw_horizontal_line(int x_position, int y_position, int length)
